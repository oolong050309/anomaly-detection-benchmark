# 本地 Streamlit 演示专用（不安装全量实验依赖）
# 适用：已有 Anaconda base 环境，只想打开交互界面看结果
#
# 用法：
#   pip install -r requirements-streamlit.txt
# 若已能 import streamlit / pandas / matplotlib / sklearn，则无需安装，直接：
#   streamlit run app/streamlit_app.py

streamlit>=1.30,<2.0
matplotlib>=3.7,<4.0
pandas>=2.0,<3.0
scikit-learn>=1.3,<2.0
