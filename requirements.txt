# Anomaly Detection Benchmark - runtime dependencies
# Target: Python 3.8 / PyTorch 2.0.0 / CUDA 11.8 / Ubuntu 20.04
#
# 注意：PyTorch / PyG / DGL 需要通过 scripts/setup_server.sh 安装（指定 CUDA 版本），
# 不要直接 pip install -r requirements.txt 装 torch（会装到 CPU 版本）。
# 本文件只列非 torch 的依赖，torch 系列由 setup_server.sh 管理。

# ---- Numerical / ML basics ----
numpy>=1.24,<2.0
pandas>=2.0,<3.0
scipy>=1.10,<2.0
scikit-learn>=1.3,<2.0

# ---- Tabular anomaly detection (shallow + deep PyOD wrappers) ----
pyod>=1.1,<2.0

# ---- Tabular supervised: gradient boosting + foundation model ----
xgboost>=2.0,<3.0
lightgbm>=4.0,<5.0
tabpfn>=2.0,<3.0

# ---- Time-series anomaly detection ----
stumpy>=1.12,<2.0
sktime>=0.30,<1.0

# ---- Graph anomaly detection ----
pygod>=1.1,<2.0

# ---- HuggingFace transformers (DADA 时序基础模型) ----
transformers>=4.30,<5.0

# ---- 以下由 scripts/setup_server.sh 安装，不在此列 ----
# torch==2.0.0+cu118
# torchvision==0.15.0+cu118
# torch-geometric==2.5.3
# pyg-lib / torch-sparse / torch-scatter (PyG whl)
# dgl (CUDA 11.8 whl)
